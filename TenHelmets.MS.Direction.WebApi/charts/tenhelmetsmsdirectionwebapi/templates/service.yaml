apiVersion: v1
kind: Service
metadata:
  name: {{ template "tenhelmetsmsdirectionwebapi.fullname" . }}
  labels:
    app: {{ template "tenhelmetsmsdirectionwebapi.name" . }}
    chart: {{ template "tenhelmetsmsdirectionwebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tenhelmetsmsdirectionwebapi.name" . }}
    release: {{ .Release.Name }}
