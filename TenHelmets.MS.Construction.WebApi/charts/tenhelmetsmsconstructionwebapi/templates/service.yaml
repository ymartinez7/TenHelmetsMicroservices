apiVersion: v1
kind: Service
metadata:
  name: {{ template "tenhelmetsmsconstructionwebapi.fullname" . }}
  labels:
    app: {{ template "tenhelmetsmsconstructionwebapi.name" . }}
    chart: {{ template "tenhelmetsmsconstructionwebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tenhelmetsmsconstructionwebapi.name" . }}
    release: {{ .Release.Name }}
