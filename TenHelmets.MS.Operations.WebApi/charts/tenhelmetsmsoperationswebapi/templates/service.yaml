apiVersion: v1
kind: Service
metadata:
  name: {{ template "tenhelmetsmsoperationswebapi.fullname" . }}
  labels:
    app: {{ template "tenhelmetsmsoperationswebapi.name" . }}
    chart: {{ template "tenhelmetsmsoperationswebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tenhelmetsmsoperationswebapi.name" . }}
    release: {{ .Release.Name }}
